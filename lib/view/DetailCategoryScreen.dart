// lib/category_detail_screen.dart
import 'package:flutter/material.dart';

class CategoryDetailScreen extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    

    return Scaffold(
      appBar: AppBar(
        title: Text("chi tiêt"),
      ),
      body: Center(
        child: Text('Details for '),
      ),
    );
  }
}
